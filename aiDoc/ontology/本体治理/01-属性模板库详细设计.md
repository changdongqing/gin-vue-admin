# 本体治理 · 01 属性模板库 详细设计

> 版本：v1.0 ｜ 日期：2026-09-06 ｜ 分支：dq ｜ 对应参考里程碑：M1（FR-1 属性模板库 + FR-5 供给接口 + FR-6 提升）
> 参考文档：dongqing `docs/ontology/本体模板化治理/本体模板化治理功能-M1详细设计.md`（芋道适配版）
> 参考前端（**权威**）：dongqing `web/apps/web-antd/src/api/ontology/propertyTemplate/index.ts` + `views/ontology/propertyTemplate/{data.ts,index.vue,modules/form.vue}`
> 本文档为详细设计，不含代码交付；文中代码为实现指引的范式示例。

---

## 目录

- [一、功能概述](#一功能概述)
- [二、术语与业务规则](#二术语与业务规则)
- [三、数据库设计](#三数据库设计)
- [四、后端设计](#四后端设计)
- [五、供给接口设计（FR-5）与提升接口（FR-6）](#五供给接口设计fr-5与提升接口fr-6)
- [六、前端设计](#六前端设计)
- [七、菜单 / API / 字典种子](#七菜单--api--字典种子)
- [八、实施步骤](#八实施步骤)
- [九、验证清单（AC 对照）](#九验证清单ac-对照)
- [十、风险与注意事项](#十风险与注意事项)

---

## 一、功能概述

### 1.1 功能定位

属性模板库是本体治理的第一块治理资产：将"属性"沉淀为可复用模板（如 `name`、`price`、`contains`），供建模侧通过供给接口拉取并挂载到本体类上（建模侧据 `templateCode` 写 `templateRef` 溯源注释）。数据属性（datatype）与对象属性（object）**同构共用一张表**，以 `kind` 字段区分：

- **datatype（数据属性）**：描述实体的数据字段，带数据类型（string/integer/decimal/boolean/datetime）、是否标识、预设 QUDT 单位 IRI（unitRef，与单位注册表联动）、枚举值；
- **object（对象属性）**：描述实体间关系，带默认基数（one-to-many/many-to-one/one-to-one/many-to-many）。

### 1.2 功能范围

**含**：
1. 属性模板全栈 CRUD（分页列表、详情、创建、更新、删除）；
2. 同义别名（alias）维护（搜索命中 + 治理合并留痕）；
3. 弃用/取消弃用（deprecated 双向切换，builtin 亦可弃用）；
4. builtin 保护（内置模板不可编辑/删除）；
5. templateCode 唯一约束（DB 部分唯一索引 + Service 校验 + 远程查重接口预留）；
6. 属性提升 promote 接口（FR-6，把建模侧属性快照提升为 custom 模板；接口预留，本期前端无入口）；
7. 供给接口 `/ontology/supply/v1/propertyTemplates`（FR-5，只读、默认排除弃用）。

**不含**：分类模板（02）、单位注册表（03，但 `unitRef` 字段已预留联动）、注释属性注册表（04）、参考本体导入（贯穿）。

### 1.3 与参考设计的差异裁定（以前端实现为准）

| # | 芋道 M1 设计 | 前端实现（**采用**） | 裁定说明 |
|---|---|---|---|
| 1 | 无 alias 字段 | 新增 `alias`（同义别名，逗号分隔，Textarea） | 别名参与 keyword 搜索命中（编码/显示名/别名三匹配），并用于治理合并留痕；建模侧挂载选择器同样按别名过滤 |
| 2 | category 7 组 | category **12 组**（前 7 组治理域原生：basic/contact/monetary/temporal/status/containment/attribution；后 5 组设备属性分组：rated/monitoring/running/management/operation） | 参考实现已导入设备属性字典（V59 种子），分组枚举扩展为 12 组 |
| 3 | disable 仅置 deprecated=1 | disable 为**幂等双向切换**（0↔1），按钮文案"弃用/取消弃用"随行状态切换 | 后端实现为读当前值取反落库 |
| 4 | keyword 匹配 templateCode 或 label | keyword 匹配 **templateCode / label / alias** 三字段 | 搜索面扩大到别名 |
| 5 | check-template-code 远程查重在表单接入 | API 已定义但**表单未接入**（后端能力预留） | 本期实现接口，前端表单不强制接入（提交时后端唯一校验兜底） |
| 6 | 治理接口 REST 风格（/create /page） | ——（见 §4.4） | 本项目统一采用 gin-vue-admin 动词式路由（createPropertyTemplate 等）；**供给接口保留 REST 版本化路径** |

---

## 二、术语与业务规则

| 术语 | 含义 |
|---|---|
| templateCode | 模板编码，全局唯一业务标识（camelCase，如 `weight`、`belongsTo`） |
| kind | 属性类型：`datatype`（数据属性）/ `object`（对象属性） |
| alias | 同义别名，逗号分隔（如 `温度值,实时温度`），供搜索命中与治理合并留痕 |
| category | 分组（12 组枚举，见 §6.3） |
| unitRef | 预设 QUDT 单位 IRI（如 `http://qudt.org/vocab/unit/KiloGM`），03 单位注册表就绪后联动校验/下拉 |
| source | 来源：`builtin`（随迁移种子分发，只读）/ `custom`（治理员创建） |
| deprecated | 弃用标记 0/1：业务弃用，模板仍在库中不推荐使用；供给接口默认排除，`includeDeprecated=true` 显式包含 |

**核心业务规则**：

1. **builtin 保护（双层）**：前端 `source === 'builtin'` 时隐藏编辑/删除按钮、禁用 templateCode 输入；后端 Update/Delete 前校验 source，builtin 返回业务错误。**builtin 可弃用**（弃用允许，编辑删除不允许）。
2. **创建强制 custom**：Service 层忽略请求中的 source，一律写 `custom`；更新时 source 不可变。
3. **kind 驱动字段显隐**：datatype 显示 type/isIdentifier/unitRef/values；object 显示 defaultCardinality。后端对"错位字段"不报错（同构存储），前端负责显隐。
4. **删除规则**：custom 模板直接软删除；后续分类模板（02）骨架引用就绪后，删除前应校验 `ont_class_template_refs` 引用（本篇预留 TODO，见 §10）。
5. **弃用 ≠ 删除**：弃用仅置位，供给默认排除；取消弃用恢复供给。

---

## 三、数据库设计

### 3.1 迁移文件

```
server/migrations/
├── 000002_create_ont_property_template.up.sql     # 建表 + 索引 + COMMENT + 字典种子 + 内置模板种子
└── 000002_create_ont_property_template.down.sql   # 回滚（DROP 表；字典种子按 type 清理）
```

> 规范：仅 PostgreSQL；`CREATE TABLE IF NOT EXISTS` 幂等；种子 `INSERT ... SELECT ... WHERE NOT EXISTS`；down 必须成对；**编写前核对 migrations 当前最大版本号**（本文档按 000002 分配）。

### 3.2 建表 DDL（up.sql）

```sql
-- ============================================================
-- 迁移: 000002_create_ont_property_template.up.sql
-- 目的: 本体属性模板表（数据属性 + 对象属性同构）+ 字典种子 + 内置模板种子
-- 说明: 主键 uint 自增（GVA_MODEL，PG 序列）；软删除 deleted_at；
--       唯一索引为部分索引 WHERE deleted_at IS NULL（逻辑删后允许编码复用）
-- ============================================================
CREATE TABLE IF NOT EXISTS ont_property_templates (
    id                  bigserial       PRIMARY KEY,
    template_code       varchar(64)     NOT NULL,
    kind                varchar(16)     NOT NULL DEFAULT 'datatype',
    label               varchar(128)    NOT NULL,
    alias               varchar(512)    NOT NULL DEFAULT '',
    description         varchar(512)    NOT NULL DEFAULT '',
    category            varchar(64)     NOT NULL DEFAULT '',
    type                varchar(32)     NOT NULL DEFAULT '',
    is_identifier       smallint        NOT NULL DEFAULT 0,
    unit_ref            varchar(255)    NOT NULL DEFAULT '',
    values              varchar(1000)   NOT NULL DEFAULT '',
    default_cardinality varchar(32)     NOT NULL DEFAULT '',
    source              varchar(16)     NOT NULL DEFAULT 'custom',
    deprecated          smallint        NOT NULL DEFAULT 0,
    status              smallint        NOT NULL DEFAULT 0,
    created_by          varchar(64)     NOT NULL DEFAULT '',
    updated_by          varchar(64)     NOT NULL DEFAULT '',
    created_at          timestamptz    NOT NULL DEFAULT now(),
    updated_at          timestamptz    NOT NULL DEFAULT now(),
    deleted_at          timestamptz
);

COMMENT ON TABLE ont_property_templates IS '本体属性模板表（数据属性 + 对象属性同构）';
COMMENT ON COLUMN ont_property_templates.template_code IS '模板编码（唯一业务标识，如 name/contains/price）';
COMMENT ON COLUMN ont_property_templates.kind IS '属性类型: datatype(数据属性)/object(对象属性)';
COMMENT ON COLUMN ont_property_templates.label IS '显示名';
COMMENT ON COLUMN ont_property_templates.alias IS '同义别名（逗号分隔），供搜索命中与治理合并留痕';
COMMENT ON COLUMN ont_property_templates.description IS '业务说明';
COMMENT ON COLUMN ont_property_templates.category IS '分组: basic/contact/monetary/temporal/status/containment/attribution/rated/monitoring/running/management/operation';
COMMENT ON COLUMN ont_property_templates.type IS '数据类型(datatype 专属): string/integer/decimal/boolean/datetime';
COMMENT ON COLUMN ont_property_templates.is_identifier IS '是否标识(datatype 专属): 0=否 1=是';
COMMENT ON COLUMN ont_property_templates.unit_ref IS '预设 QUDT 单位 IRI（与单位注册表联动）';
COMMENT ON COLUMN ont_property_templates.values IS '枚举值(逗号分隔, datatype 专属)';
COMMENT ON COLUMN ont_property_templates.default_cardinality IS '默认基数(object 专属): one-to-many/many-to-one/one-to-one/many-to-many';
COMMENT ON COLUMN ont_property_templates.source IS '来源: builtin(内置)/custom(自定义)';
COMMENT ON COLUMN ont_property_templates.deprecated IS '是否弃用: 0=否 1=是';
COMMENT ON COLUMN ont_property_templates.status IS '业务启停: 0=正常 1=停用';
COMMENT ON COLUMN ont_property_templates.created_by IS '创建人（用户名，API 层填充）';
COMMENT ON COLUMN ont_property_templates.updated_by IS '更新人（用户名，API 层填充）';

CREATE INDEX IF NOT EXISTS idx_ont_prop_tpl_kind ON ont_property_templates (kind) WHERE deleted_at IS NULL;
CREATE INDEX IF NOT EXISTS idx_ont_prop_tpl_category ON ont_property_templates (category) WHERE deleted_at IS NULL;
-- 唯一业务编码：软删除后允许复用
CREATE UNIQUE INDEX IF NOT EXISTS uk_ont_prop_tpl_code
    ON ont_property_templates (template_code) WHERE deleted_at IS NULL;
```

> `values` 为 SQL 保留字风险低（非保留关键字），但列名仍建议实现时验证；GORM 侧列名由 tag 显式指定。

### 3.3 down.sql

```sql
DROP TABLE IF EXISTS ont_property_templates;
-- 字典种子回滚（幂等）
DELETE FROM sys_dictionary_details WHERE sys_dictionary_id IN (SELECT id FROM sys_dictionaries WHERE type IN ('ont_property_kind','ont_source'));
DELETE FROM sys_dictionaries WHERE type IN ('ont_property_kind','ont_source');
```

### 3.4 GORM Model

文件：`server/model/ontology/ont_property_template.go`

```go
package ontology

import "github.com/flipped-aurora/gin-vue-admin/server/global"

// OntPropertyTemplate 本体属性模板（数据属性 + 对象属性同构，kind 区分）
type OntPropertyTemplate struct {
	global.GVA_MODEL
	TemplateCode       string `json:"templateCode" form:"templateCode" gorm:"column:template_code;size:64;not null;uniqueIndex:uk_ont_prop_tpl_code,where:deleted_at IS NULL;comment:模板编码（唯一业务标识）" binding:"required"` //nolint:stylecheck
	Kind               string `json:"kind" form:"kind" gorm:"column:kind;size:16;not null;default:datatype;comment:属性类型 datatype/object" binding:"required,oneof=datatype object"`
	Label              string `json:"label" form:"label" gorm:"column:label;size:128;not null;comment:显示名" binding:"required"`
	Alias              string `json:"alias" form:"alias" gorm:"column:alias;size:512;comment:同义别名（逗号分隔）"`
	Description        string `json:"description" form:"description" gorm:"column:description;size:512;comment:业务说明"`
	Category           string `json:"category" form:"category" gorm:"column:category;size:64;comment:分组"`
	Type               string `json:"type" form:"type" gorm:"column:type;size:32;comment:数据类型(datatype 专属)"`
	IsIdentifier       int    `json:"isIdentifier" form:"isIdentifier" gorm:"column:is_identifier;default:0;comment:是否标识 0/1"`
	UnitRef            string `json:"unitRef" form:"unitRef" gorm:"column:unit_ref;size:255;comment:预设 QUDT 单位 IRI"`
	Values             string `json:"values" form:"values" gorm:"column:values;size:1000;comment:枚举值(逗号分隔)"`
	DefaultCardinality string `json:"defaultCardinality" form:"defaultCardinality" gorm:"column:default_cardinality;size:32;comment:默认基数(object 专属)"`
	Source             string `json:"source" form:"source" gorm:"column:source;size:16;not null;default:custom;comment:来源 builtin/custom"`
	Deprecated         int    `json:"deprecated" form:"deprecated" gorm:"column:deprecated;default:0;comment:是否弃用 0/1"`
	Status             int    `json:"status" form:"status" gorm:"column:status;default:0;comment:业务启停 0/1"`
	CreatedBy          string `json:"createdBy" form:"createdBy" gorm:"column:created_by;size:64;comment:创建人"`
	UpdatedBy          string `json:"updatedBy" form:"updatedBy" gorm:"column:updated_by;size:64;comment:更新人"`
}

func (OntPropertyTemplate) TableName() string { return "ont_property_templates" }
```

> 审计填充：`CreatedBy/UpdatedBy` 由 API 层从 JWT 取用户名写入（见 §4.4），对齐本项目数据权限模块 `DeptId/CreateUserId` 的填充模式。

### 3.5 种子数据

**内置模板（≥12 条，source='builtin'）**，写于 000002 up.sql 末尾：

| templateCode | kind | label | category | type | unitRef | defaultCardinality |
|---|---|---|---|---|---|---|
| name | datatype | 名称 | basic | string | — | — |
| description | datatype | 描述 | basic | string | — | — |
| email | datatype | 邮箱 | contact | string | — | — |
| phone | datatype | 电话 | contact | string | — | — |
| price | datatype | 价格 | monetary | decimal | `.../unit/USD` | — |
| amount | datatype | 金额 | monetary | decimal | `.../unit/USD` | — |
| createdDate | datatype | 创建日期 | temporal | datetime | — | — |
| isActive | datatype | 是否启用 | status | boolean | — | — |
| length | datatype | 长度 | basic | decimal | `http://qudt.org/vocab/unit/M` | — |
| contains | object | 包含 | containment | — | — | one-to-many |
| belongsTo | object | 属于 | attribution | — | — | many-to-one |
| hasPart | object | 拥有部件 | containment | — | — | one-to-many |
| references | object | 引用 | attribution | — | — | many-to-one |

种子 SQL 范式（幂等）：

```sql
INSERT INTO ont_property_templates (template_code, kind, label, category, type, is_identifier, unit_ref, source)
SELECT 'name', 'datatype', '名称', 'basic', 'string', 1, '', 'builtin'
WHERE NOT EXISTS (SELECT 1 FROM ont_property_templates WHERE template_code = 'name' AND deleted_at IS NULL);
-- …其余条目同构…
```

**字典种子**（`sys_dictionaries` + `sys_dictionary_details`，供前端 `getDict`/`gvaDict` 渲染）：

| 字典 type | 明细（value → label） |
|---|---|
| `ont_property_kind` | datatype → 数据属性；object → 对象属性 |
| `ont_source` | builtin → 内置；custom → 自定义 |

```sql
-- 字典头（幂等）
INSERT INTO sys_dictionaries (name, type, status, "desc", created_at, updated_at)
SELECT '本体属性类型', 'ont_property_kind', true, '数据属性/对象属性', now(), now()
WHERE NOT EXISTS (SELECT 1 FROM sys_dictionaries WHERE type = 'ont_property_kind');
-- 明细（关联字典 id，幂等按 label+value）
INSERT INTO sys_dictionary_details (label, value, status, sort, sys_dictionary_id, created_at, updated_at)
SELECT '数据属性', 'datatype', true, 1, (SELECT id FROM sys_dictionaries WHERE type = 'ont_property_kind'), now(), now()
WHERE NOT EXISTS (
    SELECT 1 FROM sys_dictionary_details d
    WHERE d.sys_dictionary_id = (SELECT id FROM sys_dictionaries WHERE type = 'ont_property_kind')
      AND d.value = 'datatype');
```

> 注意 `sys_dictionaries.status` 为 boolean 列，PG 中列名 `desc` 需加双引号。实现时以 baseline DDL 实际列名为准。

---

## 四、后端设计

### 4.1 领域骨架与文件清单（本功能新增）

本功能同时建立 `ontology` 领域骨架（后续 02/03/04 复用）：

```
server/
├── model/ontology/
│   ├── ont_property_template.go            # ① 实体（§3.4）
│   └── request/ont_property_template.go    # ② 请求结构体
├── service/ontology/
│   ├── enter.go                            # ③ 领域服务聚合（OntologyServiceGroup）
│   └── ont_property_template.go            # ④ Service
├── api/v1/ontology/
│   ├── enter.go                            # ⑤ API 聚合（OntologyApiGroup）
│   └── ont_property_template.go            # ⑥ API
├── router/ontology/
│   ├── enter.go                            # ⑦ 路由聚合（RouterGroup）
│   └── ont_property_template.go            # ⑧ Router
└── initialize/
    ├── ontology_seed.go                    # ⑨ 菜单/API/casbin 幂等种子（§七）
    ├── gorm.go / ensure_tables.go          # ⑩ 两份表清单各追加 OntPropertyTemplate{}
    └── router.go                           # ⑪ PrivateGroup 上注册 ontologyRouter.InitOntologyRouter
```

聚合注册（三处 enter + 两处汇总）：

```go
// server/service/ontology/enter.go
package ontology

type ServiceGroup struct {
	PropertyTemplateService
}
var App = new(ServiceGroup) // 由 service/enter.go 挂载

// server/service/enter.go 追加
type ServiceGroup struct {
	SystemServiceGroup   system.ServiceGroup
	ExampleServiceGroup  example.ServiceGroup
	OntologyServiceGroup ontology.ServiceGroup   // ← 新增
}

// server/api/v1/ontology/enter.go、server/router/ontology/enter.go 同理
// （api 层持有 var propertyTemplateService = service.ServiceGroupApp.OntologyServiceGroup.PropertyTemplateService）
```

### 4.2 请求/响应结构体

文件：`server/model/ontology/request/ont_property_template.go`

```go
package request

import "github.com/flipped-aurora/gin-vue-admin/server/model/common/request"

// SearchPropertyTemplate 属性模板分页查询
type SearchPropertyTemplate struct {
	request.PageInfo                                  // page/pageSize/keyword
	Kind       string `form:"kind"`                   // datatype/object（可选）
	Category   string `form:"category"`               // 分组（可选）
	Source     string `form:"source"`                 // builtin/custom（可选）
	Deprecated *int   `form:"deprecated"`             // 0/1（可选，指针区分未传与 0）
}

// PropertyTemplateOps 单条操作（删除/详情/弃用）
type PropertyTemplateOps struct {
	ID uint `json:"ID" form:"ID" binding:"required"`
}

// CheckPropertyTemplateCode 编码远程查重（预留）
type CheckPropertyTemplateCode struct {
	TemplateCode string `form:"templateCode" binding:"required"`
	ExcludeID    uint   `form:"excludeId"`
}
```

> 创建/更新直接绑定实体 `ontology.OntPropertyTemplate`（ShouldBindJSON），与公司模块范式一致；source/deprecated/status 由 Service 强制处理，不信任请求值。

### 4.3 Service 层关键逻辑

文件：`server/service/ontology/ont_property_template.go`

```go
package ontology

import (
	"errors"

	"github.com/flipped-aurora/gin-vue-admin/server/global"
	"github.com/flipped-aurora/gin-vue-admin/server/model/ontology"
	ontReq "github.com/flipped-aurora/gin-vue-admin/server/model/ontology/request"
	"gorm.io/gorm"
)

type PropertyTemplateService struct{}

// CreatePropertyTemplate 创建（强制 source=custom）
func (s *PropertyTemplateService) CreatePropertyTemplate(t *ontology.OntPropertyTemplate, operator string) error {
	if err := s.validateTemplateCodeUnique(global.GVA_DB, t.TemplateCode, 0); err != nil {
		return err
	}
	t.Source = "custom" // 治理员创建一律 custom，builtin 仅迁移种子分发
	if t.Deprecated == 0 { t.Deprecated = 0 } // 默认 0（显式化）
	t.Status = 0
	t.CreatedBy, t.UpdatedBy = operator, operator
	return global.GVA_DB.Create(t).Error
}

// UpdatePropertyTemplate 更新（builtin 拒绝；source/编码归属不可变）
func (s *PropertyTemplateService) UpdatePropertyTemplate(t *ontology.OntPropertyTemplate, operator string) error {
	var existing ontology.OntPropertyTemplate
	if err := global.GVA_DB.First(&existing, t.ID).Error; err != nil {
		return errors.New("属性模板不存在")
	}
	if existing.Source == "builtin" {
		return errors.New("内置属性模板不可编辑")
	}
	if err := s.validateTemplateCodeUnique(global.GVA_DB, t.TemplateCode, t.ID); err != nil {
		return err
	}
	return global.GVA_DB.Transaction(func(tx *gorm.DB) error {
		// 用 map 更新避免结构体 Updates 忽略零值（deprecated=0 等场景）
		return tx.Model(&ontology.OntPropertyTemplate{}).Where("id = ?", t.ID).Updates(map[string]interface{}{
			"template_code": t.TemplateCode, "kind": t.Kind, "label": t.Label, "alias": t.Alias,
			"description": t.Description, "category": t.Category, "type": t.Type,
			"is_identifier": t.IsIdentifier, "unit_ref": t.UnitRef, "values": t.Values,
			"default_cardinality": t.DefaultCardinality, "deprecated": t.Deprecated,
			"updated_by": operator,
		}).Error
	})
}

// DeletePropertyTemplate 删除（builtin 拒绝；软删除）
// TODO(02 分类模板就绪后): 删除前校验 ont_class_template_refs 引用计数，被引用则拒绝
func (s *PropertyTemplateService) DeletePropertyTemplate(id uint) error {
	var existing ontology.OntPropertyTemplate
	if err := global.GVA_DB.First(&existing, id).Error; err != nil {
		return errors.New("属性模板不存在")
	}
	if existing.Source == "builtin" {
		return errors.New("内置属性模板不可删除")
	}
	return global.GVA_DB.Delete(&ontology.OntPropertyTemplate{}, id).Error
}

// DisablePropertyTemplate 弃用/取消弃用（幂等切换；builtin 也可弃用）
func (s *PropertyTemplateService) DisablePropertyTemplate(id uint, operator string) error {
	var existing ontology.OntPropertyTemplate
	if err := global.GVA_DB.First(&existing, id).Error; err != nil {
		return errors.New("属性模板不存在")
	}
	target := 1 - existing.Deprecated // 0→1 弃用，1→0 取消弃用
	return global.GVA_DB.Model(&ontology.OntPropertyTemplate{}).Where("id = ?", id).
		Updates(map[string]interface{}{"deprecated": target, "updated_by": operator}).Error
}

// GetPropertyTemplateList 分页查询（keyword 匹配编码/显示名/别名）
func (s *PropertyTemplateService) GetPropertyTemplateList(info ontReq.SearchPropertyTemplate) (list []ontology.OntPropertyTemplate, total int64, err error) {
	db := global.GVA_DB.Model(&ontology.OntPropertyTemplate{})
	if info.Kind != "" { db = db.Where("kind = ?", info.Kind) }
	if info.Category != "" { db = db.Where("category = ?", info.Category) }
	if info.Source != "" { db = db.Where("source = ?", info.Source) }
	if info.Deprecated != nil { db = db.Where("deprecated = ?", *info.Deprecated) }
	if info.Keyword != "" {
		kw := "%" + info.Keyword + "%"
		db = db.Where("template_code LIKE ? OR label LIKE ? OR alias LIKE ?", kw, kw, kw)
	}
	if err = db.Count(&total).Error; err != nil { return }
	err = db.Order("id DESC").Limit(info.PageSize).Offset((info.Page - 1) * info.PageSize).Find(&list).Error
	return
}

// GetPropertyTemplateAll 全量列表（下拉/选择器用，可按 kind 过滤，默认排除弃用）
func (s *PropertyTemplateService) GetPropertyTemplateAll(kind string) (list []ontology.OntPropertyTemplate, err error) {
	db := global.GVA_DB.Where("deprecated = 0 AND status = 0")
	if kind != "" { db = db.Where("kind = ?", kind) }
	err = db.Order("kind ASC, category ASC, id ASC").Find(&list).Error
	return
}

// GetPropertyTemplatesForSupply 供给查询（FR-5）：默认排除弃用、仅启用
func (s *PropertyTemplateService) GetPropertyTemplatesForSupply(kind, category string, includeDeprecated bool) (list []ontology.OntPropertyTemplate, err error) {
	db := global.GVA_DB.Where("status = 0")
	if !includeDeprecated { db = db.Where("deprecated = 0") }
	if kind != "" { db = db.Where("kind = ?", kind) }
	if category != "" { db = db.Where("category = ?", category) }
	err = db.Order("kind ASC, category ASC, id ASC").Find(&list).Error
	return
}

// IsTemplateCodeUnique 编码远程查重（true=可用）
func (s *PropertyTemplateService) IsTemplateCodeUnique(code string, excludeID uint) (bool, error) {
	err := s.validateTemplateCodeUnique(global.GVA_DB, code, excludeID)
	if err == nil { return true, nil }
	if errors.Is(err, errCodeDuplicated) { return false, nil } // 见下方包级哨兵错误
	return false, err
}

var errCodeDuplicated = errors.New("属性模板编码已存在")

func (s *PropertyTemplateService) validateTemplateCodeUnique(db *gorm.DB, code string, excludeID uint) error {
	var count int64
	q := db.Model(&ontology.OntPropertyTemplate{}).Where("template_code = ?", code)
	if excludeID > 0 { q = q.Where("id <> ?", excludeID) }
	if err := q.Count(&count).Error; err != nil { return err }
	if count > 0 { return errCodeDuplicated }
	return nil
}
```

> 分页排序固定 `id DESC`（前端无动态排序诉求）；如后续开放排序，按 `sys_user` 模式加排序字段白名单防注入。

### 4.4 API 层与路由

文件：`server/api/v1/ontology/ont_property_template.go`（范式同 `sys_company.go`）

```go
// CreatePropertyTemplate 创建属性模板
// @Tags      本体属性模板
// @Summary   创建属性模板
// @Security  ApiKeyAuth
// @Accept    application/json
// @Produce   application/json
// @Param     data body ontology.OntPropertyTemplate true "属性模板（source 由服务端强制 custom）"
// @Success   200 {object} response.Response "{code:0,msg:创建成功}"
// @Router    /ontology/propertyTemplate/createPropertyTemplate [post]
func (api *OntPropertyTemplateApi) CreatePropertyTemplate(c *gin.Context) {
	var t ontology.OntPropertyTemplate
	if err := c.ShouldBindJSON(&t); err != nil {
		response.FailWithMessage(err.Error(), c); return
	}
	if err := propertyTemplateService.CreatePropertyTemplate(&t, utils.GetUserInfo(c).Username); err != nil {
		global.GVA_LOG.Error("创建失败!", zap.Error(err))
		response.FailWithMessage("创建失败:"+err.Error(), c); return
	}
	response.OkWithMessage("创建成功", c)
}
```

路由注册（`server/router/ontology/ont_property_template.go`）：

```go
func (r *OntPropertyTemplateRouter) InitOntologyRouter(Router *gin.RouterGroup, PublicRouter *gin.RouterGroup) {
	priv := Router.Group("ontology")                      // JWT + casbin 已在 PrivateGroup 挂载
	write := priv.Group("propertyTemplate").Use(middleware.OperationRecord())
	read  := priv.Group("propertyTemplate")
	{
		write.POST("createPropertyTemplate", ontApi.CreatePropertyTemplate)
		write.PUT("updatePropertyTemplate", ontApi.UpdatePropertyTemplate)
		write.PUT("disablePropertyTemplate", ontApi.DisablePropertyTemplate)
		write.DELETE("deletePropertyTemplate", ontApi.DeletePropertyTemplate)
		write.POST("promotePropertyTemplate", ontApi.PromotePropertyTemplate) // FR-6 预留
	}
	{
		read.GET("findPropertyTemplate", ontApi.FindPropertyTemplate)
		read.GET("getPropertyTemplateList", ontApi.GetPropertyTemplateList)   // 分页
		read.GET("getPropertyTemplateAll", ontApi.GetPropertyTemplateAll)     // 下拉（?kind=）
		read.GET("checkPropertyTemplateCode", ontApi.CheckPropertyTemplateCode)
	}
}
```

**API 清单**：

| 方法 | 路径 | 入参 | 说明 | OperationRecord |
|---|---|---|---|---|
| POST | `/ontology/propertyTemplate/createPropertyTemplate` | body 实体 | 创建（强制 custom） | ✓ |
| PUT | `/ontology/propertyTemplate/updatePropertyTemplate` | body 实体（含 ID） | 更新（builtin 拒绝） | ✓ |
| DELETE | `/ontology/propertyTemplate/deletePropertyTemplate` | query `ID` | 删除（builtin 拒绝，软删） | ✓ |
| PUT | `/ontology/propertyTemplate/disablePropertyTemplate` | query `ID` | 弃用/取消弃用（toggle） | ✓ |
| GET | `/ontology/propertyTemplate/findPropertyTemplate` | query `ID` | 详情 | — |
| GET | `/ontology/propertyTemplate/getPropertyTemplateList` | query Search | 分页（keyword 三匹配） | — |
| GET | `/ontology/propertyTemplate/getPropertyTemplateAll` | query `kind` | 全量下拉（排除弃用） | — |
| GET | `/ontology/propertyTemplate/checkPropertyTemplateCode` | query `templateCode,excludeId` | 编码查重（返回 `{code:0,data:{usable:true}}`） | — |
| POST | `/ontology/propertyTemplate/promotePropertyTemplate` | body PromoteReq | 提升（FR-6，§5.2） | ✓ |

> 分页响应统一 `response.OkWithDetailed(response.PageResult{List, Total, Page, PageSize}, "获取成功", c)`，与 GvaGrid 分页协议 `{code:0,data:{list,total}}` 对接。

---

## 五、供给接口设计（FR-5）与提升接口（FR-6）

### 5.1 供给接口（只读、版本化）

路径：`GET /ontology/supply/v1/propertyTemplates?kind=&category=&includeDeprecated=`

- 返回：`response.OkWithData(list)`，字段同实体（含 templateCode/kind/label/alias/category/type/isIdentifier/unitRef/values/defaultCardinality/deprecated）；
- 默认 `includeDeprecated=false`（排除弃用）且仅 `status=0`；建模侧挂载选择器固定传 `includeDeprecated=false`；
- **版本化约定**：`/supply/v1/` 前缀隔离治理侧表结构演进，破坏性变更走 v2；
- 路由注册在供给路由组（无 OperationRecord），权限登记为独立 API「本体供给」组（授予建模师角色，与治理 CRUD 分离）；
- 建模侧消费方式（后续功能参考）：画布属性抽屉/批量挂载选择器拉取后按 `templateCode/label/alias` 本地过滤，选中后调实例化接口挂 `templateRef` 溯源。

### 5.2 提升接口（FR-6，预留）

`POST /ontology/propertyTemplate/promotePropertyTemplate`：接收属性快照（name 必填，templateCode 空则从 name 派生 camelCase），查重后生成 `source='custom'` 模板；重复编码返回业务错误不覆盖。本期前端无入口，接口按此实现供建模侧后续调用。

---

## 六、前端设计

### 6.1 文件清单

```
web/src/
├── api/ontology/propertyTemplate.js
└── view/ontology/propertyTemplate/propertyTemplate.vue    # 菜单 component 指向此路径
```

### 6.2 API 封装（web/src/api/ontology/propertyTemplate.js）

```js
import service from '@/utils/request'

export const getPropertyTemplateList = (params) => service({ url: '/ontology/propertyTemplate/getPropertyTemplateList', method: 'get', params })
export const getPropertyTemplateAll = (params) => service({ url: '/ontology/propertyTemplate/getPropertyTemplateAll', method: 'get', params })
export const findPropertyTemplate = (params) => service({ url: '/ontology/propertyTemplate/findPropertyTemplate', method: 'get', params })
export const createPropertyTemplate = (data) => service({ url: '/ontology/propertyTemplate/createPropertyTemplate', method: 'post', data })
export const updatePropertyTemplate = (data) => service({ url: '/ontology/propertyTemplate/updatePropertyTemplate', method: 'put', data })
export const deletePropertyTemplate = (params) => service({ url: '/ontology/propertyTemplate/deletePropertyTemplate', method: 'delete', params })
export const disablePropertyTemplate = (params) => service({ url: '/ontology/propertyTemplate/disablePropertyTemplate', method: 'put', params })
// 供给（建模侧用）
export const getPropertyTemplatesForSupply = (params) => service({ url: '/ontology/supply/v1/propertyTemplates', method: 'get', params })
```

### 6.3 列表页（GvaGrid）

`useGvaGrid` 配置要点（范式参照 `view/superAdmin/company/company.vue`）：

- `id: 'ontology-propertyTemplate'`（列设置持久化 key）；
- `api: getPropertyTemplateList`，`defaultSort: { field: 'ID', order: 'desc' }`；
- 搜索表单 `searchItems`（VxeInput/VxeSelect，span 6）：

| field | title | 组件 | 说明 |
|---|---|---|---|
| keyword | 关键字 | VxeInput | placeholder「模板编码、显示名或同义别名」，clearable |
| kind | 属性类型 | VxeSelect | options 由 `getDict('ont_property_kind')` 转换；clearable |
| category | 分组 | VxeSelect | 12 组静态 options（§3.5 注释同款枚举）；clearable |
| source | 来源 | VxeSelect | `getDict('ont_source')`；clearable |

- 表格 `columns`：

| field | title | 渲染 |
|---|---|---|
| templateCode | 模板编码（140） | — |
| kind | 属性类型（100） | `cellRender: { name: 'gvaDict', props: { dict: 'ont_property_kind' } }` |
| label | 显示名（120） | — |
| alias | 同义别名（140） | showOverflow |
| category | 分组（90） | — |
| type | 数据类型（90） | — |
| unitRef | 预设单位（160） | showOverflow |
| defaultCardinality | 基数（100） | — |
| source | 来源（80） | `gvaDict: ont_source` |
| deprecated | 弃用（80） | slot：el-tag（0=正常 success / 1=已弃用 info） |
| CreatedAt | 创建时间（170） | `gvaDate` 渲染器 |
| （操作） | 操作（200） | fixed right，slot `operate` |

- 工具栏：「新增属性模板」primary 按钮（`openForm('add')`）；
- 操作列（`#operate` 插槽）：

| 按钮 | 显示条件 | 行为 |
|---|---|---|
| 编辑 | `row.source !== 'builtin'` | `openForm('edit', row)` |
| 弃用 / 取消弃用（动态文案 `row.deprecated === 1 ? '取消弃用' : '弃用'`） | 恒显示（builtin 也可弃用） | 调 `disablePropertyTemplate({ID: row.ID})`，成功提示 `已弃用「label」/已取消弃用「label」` 后 `refresh()` |
| 删除 | `row.source !== 'builtin'` | `ElMessageBox.confirm` 确认后调 delete，`reload()` |

> 按钮级权限为可选增强（角色-按钮表）；后端 casbin 按 (path,method) 兜底鉴权，前端不因缺按钮权限而隐藏时仍安全。

### 6.4 表单（el-drawer）

`el-drawer`（`:size="appStore.drawerSize"`）+ `el-form`，`dialogType`（add/edit）区分标题与提交接口；编辑时先 `findPropertyTemplate` 拉详情回填（行数据可能不含全字段）。

| 字段 | 组件 | 必填 | 说明 |
|---|---|---|---|
| templateCode | el-input | ✓ | placeholder「唯一标识，如 weight」；编辑 builtin 时 disabled（本功能 builtin 不可编辑，防御性保留） |
| kind | el-select | ✓ | datatype 数据属性 / object 对象属性；**change 时重算动态字段显隐** |
| label | el-input | ✓ | 如 重量 |
| alias | el-input（textarea, rows 2） | — | 「逗号分隔，如 温度值,实时温度（供搜索命中与治理合并留痕）」 |
| description | el-input（textarea, rows 2） | — | — |
| category | el-select（clearable） | — | 12 组 options |
| type | el-select（clearable） | — | `v-if="form.kind === 'datatype'"`：string/integer/decimal/boolean/datetime |
| isIdentifier | el-switch（1/0） | — | `v-if` datatype |
| unitRef | el-input | — | `v-if` datatype；placeholder「http://qudt.org/vocab/unit/KiloGM（可选，单位注册表就绪后联动）」；03 交付后改为按量纲过滤的单位下拉 |
| values | el-input | — | `v-if` datatype；「逗号分隔，如 HIGH,MEDIUM,LOW」 |
| defaultCardinality | el-select（clearable） | — | `v-if="form.kind === 'object'"`：one-to-many 一对多 / many-to-one 多对一 / one-to-one 一对一 / many-to-many 多对多 |

- 新增默认值：`{ kind: 'datatype', isIdentifier: 0, deprecated: 0, status: 0 }`（source 隐藏，服务端强制 custom）；
- 提交：`formRef.validate` → create/update → `res.code === 0` 则 `ElMessage.success` + `refresh()` + 关抽屉；
- 编码查重（可选增强）：templateCode blur 时调 `checkPropertyTemplateCode`，非空红字提示「编码已存在」。

---

## 七、菜单 / API / 字典种子

文件：`server/initialize/ontology_seed.go`（范式参照 `data_permission.go`，`main.go` 在 `SeedDataPermission()` 后调用，幂等）：

1. **菜单**：顶级目录「本体治理」（name=`ontology`，path=`/ontology`，component=`view/routerHolder.vue`，icon 建议 `share`/`link`）下追加子菜单：
   - name=`propertyTemplate`，title=属性模板库，component=`view/ontology/propertyTemplate/propertyTemplate.vue`；
   - 并为 888 角色插 `sys_authority_menus`；
2. **API + casbin**：`sys_apis` 登记 §4.4 清单 + 供给接口（group=本体治理/本体供给），`casbin_rule` 插 `p, 888, /path, METHOD`，新增后 `FreshCasbin()` 热加载；
3. **字典**：已随迁移 SQL 种入（§3.5），Go 种子不做（避免双写漂移）。

> 供给接口的 casbin 规则同时授予"建模师"角色（若该角色已存在）；建模侧角色与治理角色权限分离。

---

## 八、实施步骤

1. 写迁移 `000002_create_ont_property_template.{up,down}.sql`（表 + 索引 + COMMENT + 字典种子 + 内置模板种子）；
2. 建 model/ontology 实体 + request；注册 `initialize/gorm.go` 与 `ensure_tables.go` 两份表清单（AutoMigrate 兜底）；
3. 建 service/api/router 三层 + 三处 enter 聚合 + `initialize/router.go` 挂载；
4. `go build ./...` 编译（迁移经 go:embed 内嵌）；启动验证日志 `current schema version=2 dirty=false`；
5. 写 `initialize/ontology_seed.go`（菜单/API/casbin），重启验证菜单可见、接口 403→200；
6. 前端 `api/ontology/propertyTemplate.js` + `view/ontology/propertyTemplate/propertyTemplate.vue`；
7. 联调过 §九 验证清单。

---

## 九、验证清单（AC 对照）

| AC | 验证方法 | 预期 |
|---|---|---|
| AC-1.1 种子 | `SELECT count(*) FROM ont_property_templates WHERE source='builtin' AND deleted_at IS NULL` | ≥12（9 datatype + 4 object） |
| AC-1.2 新增 | 前端新增 weight（datatype/decimal/unitRef=KiloGM IRI） | 持久化，source='custom'，created_by=当前用户 |
| AC-1.3 唯一 | 重复 templateCode 创建 | `code=7`，msg 含「属性模板编码已存在」（非 500） |
| AC-1.4 过滤 | `?kind=object` 分页 | 结果全部 kind=object |
| AC-1.5 弃用 | 弃用后再「取消弃用」 | deprecated 0→1→0；供给默认排除/包含切换正确 |
| AC-1.6 builtin | 编辑/删除 builtin 行 | 后端拒绝（前端按钮已隐藏，直连接口仍拒绝） |
| AC-1.7 删除 | 删除 custom 模板 | 软删除（deleted_at 置位），同编码可重新创建 |
| AC-1.8 供给字段 | `GET /ontology/supply/v1/propertyTemplates` | 含 templateCode/kind/label/alias/type/unitRef/defaultCardinality |
| 别名搜索 | keyword=别名片段 | 命中 alias 匹配的模板 |
| AC-5.1 供给权限 | 建模师角色调供给 | 可访问；调治理 create 被拒 |
| AC-5.6 弃用排除 | 供给默认 vs `includeDeprecated=true` | 默认排除 deprecated=1 |
| AC-5.7 版本化 | 路径 | `/ontology/supply/v1/propertyTemplates` |
| AC-6.2 提升查重 | promote 重复编码 | 业务错误不覆盖 |
| 审计 | create/update 后查 created_by/updated_by | 当前登录用户名 |

---

## 十、风险与注意事项

| # | 风险/注意 | 应对 |
|---|---|---|
| 1 | **`values` 列名** | SQL 非保留字但易混淆，GORM tag 显式 `column:values`；迁移执行后 `\d ont_property_templates` 核对 |
| 2 | **部分唯一索引 vs AutoMigrate** | gorm tag 的 `uniqueIndex,where:` 在 AutoMigrate 下对 PG 支持有限——**以迁移 SQL 的部分索引为准**，gorm tag 仅作开发期兜底描述；两者冲突时以迁移为准（长期规范：结构变更只走迁移） |
| 3 | **disable 语义差异** | 本功能 disable=弃用 toggle（deprecated），03 单位=停用 toggle（status）；API/前端命名保持 `disableXxx` 一致但内部落库字段不同，实现时勿混淆 |
| 4 | **删除引用校验缺失** | 02 分类模板骨架（ont_class_template_refs.property_template_code）就绪后必须补引用计数校验，否则删除模板会留下悬挂引用 |
| 5 | **operator 填充** | `utils.GetUserInfo(c).Username` 可能为空（历史 token），Service 对空 operator 容忍（落空串），不阻断业务 |
| 6 | **供给无分页** | 模板量级预期 ≤ 数百，全量返回；超千级时再加游标/分页（v2 契约） |
| 7 | **软删 + 唯一索引** | 部分索引 `WHERE deleted_at IS NULL` 保证逻辑删后编码可复用；Service 查重条件同样受 GORM 软删自动过滤，口径一致 |
| 8 | **字典列名** | `sys_dictionaries.desc` 在 PG 中需双引号；种子 SQL 实现前先对照 baseline 实际 DDL |

---

*本文档基于 dongqing M1 详细设计（业务规则/AC）与东清前端实现（字段/交互权威），按 gin-vue-admin 分层规范（AGENT.MD + aiDoc/modules/backend-layer-rules.md + 公司模块范式）适配编写。实施时同步遵循 `aiDoc/db-migration/README.md` 迁移 SOP。*
